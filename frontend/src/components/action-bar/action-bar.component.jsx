import { ActionBarContainer } from "./action-bar.styles";
const ActionBar = () => {
	return (
		<ActionBarContainer>
			<span>Acciones</span>
		</ActionBarContainer>
	);
};

export default ActionBar;
